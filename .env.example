SLACK_BOT_TOKEN=
SLACK_APP_TOKEN=
SLACK_SIGNING_SECRET=
BOT_NAME=Sniptail
DEBUG_JOB_SPEC_MESSAGES=false
ADMIN_USER_IDS=
SNIPTAIL_DRY_RUN=0
REDIS_URL=
OPENAI_API_KEY=
# Optional; required for GitLab merge requests or repository bootstrap.
GITLAB_BASE_URL=
GITLAB_TOKEN=
# Optional; required for GitHub pull requests or repository bootstrap.
GITHUB_API_TOKEN=
GITHUB_API_BASE_URL=https://api.github.com
REPO_CACHE_ROOT=
JOB_WORK_ROOT=
JOB_REGISTRY_PATH=./data/job-registry
# Job registry storage type: sqlite (default) or pg.
# Note: Drizzle migrations are expected to be run externally (not at app startup).
JOB_REGISTRY_DB=sqlite
# Required when JOB_REGISTRY_DB=pg.
JOB_REGISTRY_PG_URL=
# Set a directory (defaults to job-registry.sqlite inside) or a sqlite file path.
REPO_ALLOWLIST_PATH=./repo-allowlist.json
JOB_ROOT_COPY_GLOB=
INCLUDE_RAW_REQUEST_IN_MR=false
PRIMARY_AGENT=codex
GH_COPILOT_EXECUTION_MODE=local
GH_COPILOT_DOCKERFILE_PATH=./Dockerfile.copilot
GH_COPILOT_DOCKER_IMAGE=snatch-copilot:local
GH_COPILOT_DOCKER_BUILD_CONTEXT=
COPILOT_IDLE_RETRIES=2
CODEX_EXECUTION_MODE=local
CODEX_DOCKERFILE_PATH=./Dockerfile.codex
CODEX_DOCKER_IMAGE=snatch-codex:local
CODEX_DOCKER_BUILD_CONTEXT=
