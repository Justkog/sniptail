# Suggested commands
## Install / dev
- `pnpm install`
- `pnpm run dev`

## Build / run
- `pnpm run build`
- `pnpm run start`

## Lint / format / check
- `pnpm run lint`
- `pnpm run format`
- `pnpm run check`

## System utilities (Linux)
- `ls`, `rg`, `find`, `sed`, `awk`, `git`