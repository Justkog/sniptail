# Versioning

- Patch: fixes + internal changes
- Minor: new features that don't break existing config/workflows
- Major: breaking config/behavior changes, removed/renamed commands, incompatible data format changes
